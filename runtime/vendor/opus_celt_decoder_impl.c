/* celt/celt_decoder.c compiled in its OWN translation unit — see
 * the header comment in opus_impl.c for why. Needs -DOPUS_BUILD
 * -DUSE_ALLOCA, same as opus_impl.c (this package's amalgame.toml
 * `cflags`). */
#include "opus/celt/celt_decoder.c"
